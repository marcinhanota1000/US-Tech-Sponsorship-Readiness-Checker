import type { CandidateProfile } from './types'

export const emptyProfile: CandidateProfile = {
  language: 'basic',
  experience: 'junior',
  demand: 'standard',
  education: 'courses',
  international: false,
}
